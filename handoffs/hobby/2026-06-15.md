# Handoff ... 2026-06-15 (Hobby)

## Shipped today

### `2026.615.1332` ... headless installer fix + full from-scratch test on valkyrie

Doug said "I need to be able to run this from scratch and have it all work ...
uninstall it if you need to. We're testing this for real." So I did, on the real
OpenClaw box (`ssh skippy@valkyrie`).

**Found a real installer bug.** Running the one-line installer over headless SSH
aborted right before setup:

```
> Setting up 2200...
/tmp/2200-install.sh: 482: cannot open /dev/tty: No such device or address
✗ Setup did not complete.
```

`install.sh` tested `[ -r /dev/tty ]` to decide whether to reattach the terminal
for setup's one interactive question. The device node passes the `-r` permission
test even with **no controlling terminal** (headless SSH, CI, cron), but `open()`
then returns ENXIO, killing the install. My first fix (`{ : < /dev/tty; }`) made it
worse under dash: a redirection error on a POSIX **special built-in** (`:`/`exec`)
force-exits a non-interactive shell, *uncatchable* by `if`/`||` — that's why a bash
`sh -n` passed but Ubuntu's dash died with status 2. Final fix probes the open in a
**subshell** `(exec < /dev/tty)` so the force-exit is contained and we fall back to
a fully non-interactive `2200 setup`. (PR #285.)

**Verified the whole thing from scratch on valkyrie** (uninstalled 2200 + wiped
state, OpenClaw left fully intact and running):

- `sh install.sh` headless (dash) → install `2026.615.1332` → OpenClaw migration
  (Skippy: 58 brain notes, 54 daily memories, 6 schedules, persona verbatim, 3 LLM
  keys) → web URL → **exit 0**.
- Daemon bound `0.0.0.0`, `staticDir` → `dist/web`; web root serves the **real app**
  (`<title>2200</title>`) over **Tailscale `100.110.13.103`** and **LAN
  `192.168.1.159`**, both HTTP 200 (these are the exact IPs that gave Doug
  ERR_CONNECTION_REFUSED / TIMED_OUT before — now fixed by the bind + serve combo).
- Skippy present, state `stopped` (where the migration intends to leave it). I did
  NOT start it: it imported 6 schedules and `agent start` could fire them / take real
  actions ... that's Doug's call after reviewing in the web app.
- OpenClaw never disabled (non-interactive setup doesn't ask), `openclaw-gateway`
  still running. Source instance untouched.

**Release mechanics:** the install.sh fix is installer-only (not in the npm
package), but `releases/latest/download/install.sh` (what 2200.ai pulls) comes from
the tagged commit. The `2026.615.1301` tag predated the fix, so I cut
`2026.615.1332` to re-attach the corrected installer as the latest release asset.
Confirmed the asset carries `(exec < /dev/tty)`. **Simon still needs to redeploy
install.sh to 2200.ai** — see `wiki/inbox/simon/2026-06-15-installsh-headless-fix-redeploy.md`.

**valkyrie end state:** working 2200 `2026.615.1301` (CLI identical to 1332 bar the
CHANGELOG), daemon on `0.0.0.0`, web app serving, Skippy migrated + stopped, OpenClaw
running. Doug's live URL:
`http://100.110.13.103:2200/?token=fdde40d31ae6f4cbeaa98318e3d85dc4febad8c5bde87266a1592cc194f0a4f3`

### `2026.615.1301` ... the web app is now actually served

**The bug Doug hit:** after install + `2200 web` (or the URL setup prints), the
browser showed *"The web app has not been built yet"* with only the bare API up.

**Root cause:** the published npm package never contained the built web UI.
`package.json#files` ships `dist/`, but the web app builds to `apps/web/dist` and
was never copied in. `tsup` runs `clean: true`, so anything staged into `dist/`
before the build is wiped. And `resolveStaticDir` only looked for `apps/web/dist`,
which isn't in the package at all.

**Fix (PR #284, merged, released, live on npm):**
- `scripts/bundle-web.mjs` copies `apps/web/dist` → `dist/web` *after* the build,
  wired into `prepack` via a new `bundle:web` script. Every pack/publish now
  carries the web app.
- `resolveStaticDir` walks up from the server module checking `dist/web` (prod)
  then `apps/web/dist` (dev), so it resolves no matter which bundle the server
  code was inlined into.
- eslint ignores `scripts/**` (plain Node `.mjs`, outside the TS project).

**Verified live from npm** (not just the local tarball): a fresh
`npm install -g @twentytwohundred/2200-cli@2026.615.1301` in a clean container,
then `2200 setup`, serves the real app (`<title>2200</title>` + hashed JS/CSS),
`staticDir` bound to `.../dist/web`. `pnpm verify:all` green (2137 + 95).

**My miss:** earlier setup verification only checked the API returned HTTP 200;
I never loaded the app HTML, so the missing static dir slipped through. Setup
verification now asserts on served app content, not just API health.

## How Doug gets it

```
npm install -g @twentytwohundred/2200-cli@latest
2200 setup
```

`2200 setup` is idempotent ... it ensures the daemon is up, bound to the LAN, and
reprints the access URL with the token. Or `2200 update` from the running install.

(2200.ai/install.sh is still stale across releases ... Simon resync pending. Until
that lands, the npm command above or the GitHub release tarball is the reliable
path. See `wiki/inbox/simon/2026-06-14-installsh-sync-lagging.md`.)

## Open follow-ups (unchanged)

- Simon: make `install.sh` sync event-driven so 2200.ai tracks each release.
- Non-identity schema migrate-on-read helper before any schema bump (forward risk).
- `--host lan|localhost|tailscale|<ip>` bind override (named, not built).
- Phase 5 feedback flywheel (triage/fixer Agents); Workstream W (2200.ai homepage
  rewrite for Normals).
