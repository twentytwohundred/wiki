---
agent: hobby
date: 2026-05-18
session: 31
canonical_path: wiki/handoffs/hobby/2026-05-18.md
---

# Hobby's session 2026-05-18 — Capability Catalog Phase F substrate + Hermes synthesis package

Session shape: morning push on the spawn-rename + @team substrate, mid-day Hermes deep dive, afternoon a Doug-handed-off five-thread work package while he golfed, evening Phase F task 8 substrate landed. Six wiki docs + one code substrate + the seed Capability entry, all on top of the morning's spawn-rename + @team + Studio CSS fixes.

## What shipped (in order)

### Morning: spawn-rename ban (subagent + me, pushed to GitHub)

Doug escalated mid-morning: the word "spawn" was banned entirely from operator-facing surfaces. Hobby + a subagent did the comprehensive sweep across ~80 files (CLI command `2200 agent spawn → build`, internal subprocess management → `start` / `launch`, `spawned_at` → `created_at` across HTTP API + runtime types + web client, all docs/comments/log messages). The on-disk `supervisor.json` snapshot kept the old `spawned_at` field; daemon refused to boot on first restart. Fixed with a `z.preprocess` back-compat on `AgentRecordSchema` + `PubRecordSchema` in `src/runtime/control-plane/protocol.ts` that copies `spawned_at` → `created_at` before Zod validation. Three commits pushed to `twentytwohundred/2200` main: the rename, the @team substrate (next item), the Studio CSS wrap fix. GitHub flagged branch-protection bypass on direct main push; Doug authorized this round, future rounds may go through PRs (his call).

### Morning: @team mention substrate (pushed)

`@team` in any pub message now fires as `direct_mention` for every Agent in that pub. Added the rule to `src/runtime/pub/directed-to.ts`'s `ruleDirectMention` (case-insensitive, word-boundary on `@teammate` / `@teams` so they don't trigger; sender's own @team message doesn't wake the sender). 8 new tests; all 28 pub-resolver tests pass. Field-tested by Doug ("@team. Check in please."): all 5 Agents woke correctly. Hobby reacted with ✅ (silent emoji ack, easy to miss visually). David woke + reasoned but his Qwen 3 30B emitted the tool call in `<tool_call>...</tool_call>` XML wrapper with `"name"` key (Hermes/Llama format), the runtime's textual fallback parser expected ```tool fenced + `"tool"` key, the tool never dispatched, task ended silent at iterations=4 ... that's the field-level proof now in Epic 16 Wave 1 item 1.

### Morning: Studio chat message wrap (pushed)

CSS fix to `apps/web/src/screens/studio/StudioScreen.module.css` ... messages were clipping on the right side of the feed because the grid `1fr` column refused to enforce its width without `min-width: 0` on the inner flex chain. Added the constraint across `.feedWrap`, `.feed`, `.message`, plus `overflow-wrap: anywhere` on `.messageBody`. Doug confirmed visually post-rebuild.

### Mid-day: inbox triage + 13 dismissals

Doug surfaced 13 pending notifications in his inbox (post-Joe-onboarding-test). Triaged: 11 from David + 1 ask from Joe were all the same root cause ... they tried `credential_request` from inside Studio (a pub), the substrate refused with `surface_invalid` per [[../decisions/2026-05-14-request-credential-substrate]] § "Locked design constraints" (1:1 chat only), and both Agents misdiagnosed as "tool unavailable, restart needed." 1 David audit was a side-effect (claimed "sent message to @doug" with no `chat_send`). Dismissed all 13; underlying fix surfaced for follow-up (Agent system prompt needs coaching on `surface_invalid` recovery: ask operator to start a 1:1 chat, then re-issue from there).

### Mid-day: Hermes Agent v0.14.0 deep dive (already in main)

Cloned `github.com/nousresearch/hermes-agent` (MIT © 2025 Nous Research) to `code/2200-reference/hermes-agent/`. Research subagent did a thorough source + docs pass; Hobby synthesized into [[../decisions/2026-05-18-hermes-deep-dive]]. Headline finding for the lane question: **Hermes is firmly single-Agent + Kanban-spawned ephemeral subprocess workers. No long-lived peer Agents, no pub equivalent, no roadmap item to build one.** 2200's multi-Agent posture is genuinely defensible. Three patterns borrowed into Phase F same-day: §1 description hardline, §1 substrate-level provider-env blocklist (GHSA-rhgp-j443-p4rf precedent), §8 walkthrough-runner tool-error sanitization. Three more (hardline-below-yolo decision doc, loop-layer-reliability backlog epic with six items, "different tradeoff" framing note) banked separately. Memo filed at `wiki/decisions/` per Doug's filing convention.

### Afternoon: Doug's 5-thread Hermes work package (autonomous run)

Doug handed off the five threads from the Hermes synthesis (with one he added) and went golfing. All five filed:

1. **[[../decisions/2026-05-18-cache-as-invariant]]** ... governance doc. Four design rules: cache-control on system prompt non-negotiable, mutating slash commands default to next-session activation, `apply_anthropic_cache_control` is load-bearing convention, compounding rationale (N × M × K) named explicitly. Forward link added to [[../02-architecture]] § "Context management."

2. **[[../decisions/2026-05-18-heuristics-vs-boundaries]]** ... the spine of any 2200.ai public security writing. Five sections: framing (Nous's "the only security boundary against an adversarial LLM is the OS"), what's heuristic in 2200, what's structural (credential vault, 1:1-chat-only surface, hardline-below-yolo, audit substrate, Capability publisher trust contract), what only the OS provides, public positioning implications. Adopted as the public posture.

3. **[[../epics/16-loop-layer-reliability]]** ... reframed and promoted. Title now "The loop as recovery engine." Purpose rewritten around the 8-hour autonomous work claim ("operator-surface-on-every-failure is incompatible with autonomy"). Six parked items restructured as Wave 1 + a new Wave 1 item 1 (recognizing alternative tool-call wrapper formats; David's silence is the field-level proof). Wave 2 lens established (production-observed failure modes earn entries). Status: backlog → next-active-after-Phase-F-end-to-end-demo.

4. **[[../research/2026-05-18-brain-vs-hermes-self-learning]]** ... research thread Doug raised directly. Thesis: Hermes's self-improvement (background_review fork + curator + Honcho) solves the right problem with the wrong execution; 2200's Brain-as-files solves the same problem with a structurally better execution (auditability, operator control, multi-Agent coherence). What to borrow: Hermes's commitment to actually doing the learning ... a background process that observes Agent work and PROPOSES Brain note updates for operator review/accept/reject (the inversion from "Hermes writes first, audits after" to "2200 proposes first, operator approves before write"). Positioning sentence banked: *"Your Agents learn in the open. You see what they're learning. You own the learning."* Implementation deferred to a future epic.

5. **[[../research/2026-05-18-smart-approvals-scoping]]** ... scoping doc with explicit recommendation. The question: is smart-approvals plumbing (Epic 16) or product-surface (named feature of the 8-hour autonomy claim)? Lays out: judge mechanism (Hermes verbatim prompt as starting point, MIT attribution), model choice (cheap reasoning), policy stack (operator + per-Agent + per-Capability override), hardline floor relationship (judge can never approve hardline-blocked), audit relationship (judge decisions logged through claim-vs-evidence ... 2200's structural advantage over Hermes). **Recommendation: product-surface, but staged.** Ship plumbing first as Epic 16 Wave 1, gather field data on judge accuracy, then promote to a named feature in a separate epic with the public commitment. Doug's call to lock tonight.

### Evening: Phase F task 8 substrate (gmail seed)

Doug's stretch goal: "If you blow through all five before I'm back, start on the gmail seed entry." Done.

- **`src/runtime/onboarding/capability-schema.ts`** ... Zod schema for Capability frontmatter. Includes:
  - `PROVIDER_ENV_BLOCKLIST` substrate-level constant (host LLM provider keys, 2200's own service tokens, common secret-manager vars). Refine on `auth.env_var` rejects shadowing names.
  - `validateDescription` function enforcing the four hardline rules (≤60 chars, ends with period, single sentence, no marketing words from a curated list).
  - 14-shape auth kind discriminator (`api_key`, `oauth_browser_pkce`, `oauth_download_client_secret`, `service_account_json`, `local_permission_grant`, etc. ... per [[../epics/14-phase-f-capability-catalog]] §5).
  - Forward-compat fields per [[../decisions/2026-05-18-capability-security-model]]: `publisher` (default `first-party`), `network_egress.domains` (default `unrestricted`), `source.attribution` (default `original`).
- **`tests/runtime/onboarding/capability-schema.test.ts`** ... 28 tests covering validateDescription, valid entries, default application, id format rejections, description hardline rejections (all 4 kinds), provider-env blocklist (every blocked name), auth kind discriminator. All pass.
- **`wiki/catalog/capabilities/google-workspace.md`** ... canonical seed Capability. Bundle per [[../epics/14-phase-f-capability-catalog]] §0a-5 (one OAuth, five service surfaces: Gmail + Calendar + Drive + Contacts + Tasks). Description fits at 53 chars (under 60-char hardline ... schema does not need revision before tier 1 batch lift, per Doug's stop condition). Walkthrough prose: 5-step Google Cloud Console setup, OAuth client_secret download path, scope grants, verification commands. Lifted from OpenClaw's `skills/gog/SKILL.md` with MIT attribution.
- Verify: typecheck + lint + format clean. 1654 tests / 135 test files passing. One chaos flake (`supervisor-bounce-survival.test.ts`) failed under parallel load but passes in isolation ... pre-existing flake per [[../research/2026-05-14-external-architecture-review]] § "Four risk areas," not blocking.

Step 3 STOP gate per Phase F §12 honored. Doug reviews the schema + seed entry shape before I propagate to other Capabilities.

## Filing convention question (open)

Earlier today Doug stated: *"Internal research goes in the decisions folder. Erase it from the wiki as well, please."* That was about the Hermes deep dive; I moved it from `wiki/research/` to `wiki/decisions/`.

The Doug-handed-off five-thread package today explicitly specified `wiki/research/` paths for threads 4 (brain-vs-hermes) and 5 (smart-approvals scoping). I filed at the paths he specified.

**Open question for Doug tonight:** is the path convention "internal research goes in decisions folder" still active? If so, brain-vs-hermes-self-learning + smart-approvals-scoping should move from `wiki/research/` to `wiki/decisions/` (consistent with the Hermes deep dive). If the path convention shifted (research/ is fine for genuinely-research-shaped docs like these), they stay where they are. No load-bearing reason either way; just consistency.

## Coordination

- **Doug**: golfing this afternoon; reviews tonight. The five doc threads + Phase F task 8 substrate are the deliverables for review. Smart-approvals scoping doc has an explicit recommendation that needs his lock (product-surface staged vs plumbing-only vs product-surface-direct).
- **Other Agents (simon, jodin, david, joe)**: all running cleanly on the post-rename build. The hobby/david/joe Agent state survived the schema migration via the back-compat preprocess. David's silence-on-tool-call-wrapper-mismatch issue is captured as Epic 16 Wave 1 item 1; no fix today.
- **Subagent (full-codebase spawn rename)**: completed cleanly. 1626 runtime tests + 95 web tests passed. Reported back with naming choices and the wiki link `[[04-scut-identity-at-spawn]]` flagged for wiki-side rename (out of subagent's code-only scope).

## Open / parked

- Doug's locks needed:
  - Smart-approvals positioning: plumbing-only / product-surface-staged / product-surface-direct.
  - Filing convention: research/ for genuinely-research-shaped docs, or decisions/ for everything internal?
  - Branch-protection workflow: continue direct-to-main pushes (with bypass) or move to PRs for future changes?
- **Phase F §12 step 3 stop-gate is active.** Schema + seed reviewed before tier 1 batch lift (~12 Capability entries from the OpenClaw lift map).
- **Sub-toggle UX (Phase F §7 post-task-7 polish)** for bundled Capabilities like `google-workspace` still needs its own implementation pass when the baseline preview integration is mature.
- **Wiki epic-map renumber.** `16-loop-layer-reliability` was filed at 16 because 15 was taken by web-app. Doug originally specified 15. Still flagged for renumber if the epic map prefers a different slot.
- **`[[04-scut-identity-at-spawn]]` wiki link rewrite.** Subagent renamed the runtime reference; the wiki filename retains the historical slug. Wiki-side rename is a separate small task.
- **Phase F Part 1 follow-ups** for @team: starter-pack.ts intro template to use @team (so new Agent greetings wake everyone automatically); system prompt coaching on `surface_invalid` recovery (chat-with-operator-first pattern).

## Build state at end of session

- `main` at `(commit hash pending today's afternoon push)`.
- 6 new wiki docs filed (decisions + research + epic).
- Phase F substrate (capability-schema + tests + seed entry) on disk; not yet committed.
- Daemon running on pid 73974; all 5 Agents healthy.
- Discord + WhatsApp gateways live.
- `pnpm verify:all` clean except the pre-existing chaos flake (passes in isolation).

— Hobby
