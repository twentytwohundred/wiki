---
agent: hobby
date: 2026-06-12
session: 38
canonical_path: wiki/handoffs/hobby/2026-06-12.md
---

# Hobby's day on 2026-06-12 ... full release review, the plan, and the first real release

Doug came in wanting out of the weeds: a complete codebase review and a clear path to release and mass adoption. The review's headline finding reframed everything: **2200 is built but has never been released.** No git tags, no GitHub Releases, npm 404s on `@twentytwohundred/2200`, and `https://2200.ai/install.sh` is live but installs a package that doesn't exist. The remaining path is not feature work.

## The review (morning)

Four parallel deep-dives (runtime core, web app + connectors, distribution, wiki/project state). Full findings folded into [[strategy/release-plan]]. Highlights:

- **Strong:** 1,849 tests green, CI clean (no skips, no `continue-on-error`), all web screens implemented with no stubs, connector security posture solid (constant-time compares, sealed AES-256-GCM stores, rate limits, audit families), install.sh handles the Ubuntu npm-prefix trap.
- **Must-fix (Phase 1):** `WebTokenStore` file-mode verification; default pub owner `'doug'` in `supervisor/pub-lifecycle.ts` (must derive from operator identity or fail fast); zero tests on `tools/perm` evaluator, connector inbound routing, control-plane, endpoints.
- **Docs gaps:** backup/restore, upgrade semantics, troubleshooting.
- **Record drift:** epic map stale since 04-29; no handoffs for the 05-22 → 06-11 sessions (~25 merged PRs). Backfill is a standing Phase 0 task.

## The plan

[[strategy/release-plan]] (v0.2, accepted same-day): Phase 0 prove the pipeline → Phase 1 harden for strangers → Phase 2 dogfooding (the Cray test, finally) → Phase 3 launch (David) → Phase 4 adoption + revenue (Epic 19 then 17; 16/13 deferred) → Phase 5 feedback flywheel (triage Agent + fixer Agent opening PRs overnight behind CI + human merge gates ... Doug's "fixes bugs while I sleep" loop). Plus Workstream W: rewrite 2200.ai for Normals (it failed the brother test), positioned against OpenClaw/Hermes, brother-test as acceptance.

## Doug's calls

- **Calendar versioning, locked**: `YYYY.M.D`, first published release is `2026.6.12`. Decision record at [[2026-06-12-calver-versioning]] (npm/semver forces no leading zeros; one release per UTC day).
- Epic 19 right after launch, managed service spec during dogfooding: standing recommendations adopted as defaults.

## What shipped (afternoon)

- **Merged the queue:** #265 (CLI sources `runtime.env`/`oauth-apps.env` on every invocation via `preAction` hook ... the 06-03 live regression), #253 (baileys rc13), #266 (12-dep minor/patch group).
- **Found and fixed a broken `main`:** the sequential dependabot squash-merges left a duplicated `ws@8.21.0` mapping key in `pnpm-lock.yaml`; frozen installs (and therefore all CI) were broken at HEAD. Regenerated on the release branch.
- **PR #267 ... `release: switch to calendar versioning, cut 2026.6.12`:** version bump, CHANGELOG roll (`[Unreleased]` → `[2026.6.12]`, entries added for #263/#264/#265, unpublished `0.1.0` kept as history), install.sh example version, `*.tgz` gitignored (stray `npm pack` artifact removed from repo root), lockfile fix.
- **The release workflow's first-ever run found a real bug.** Three consecutive failures on the `supervisor-bounce-survival` chaos test that looked like the old #234 flake were actually deterministic: `pnpm verify` ran tests BEFORE build, and the chaos test spawns a real Agent from `dist/runtime/agent/bootstrap.js` ... fresh runner, no `dist/`, instant spawn death, 20s predicate timeout. `ci.yml` had been quietly immune via an explicit Build-before-Test step (with a comment incorrectly claiming verify matched). **PR #268** reorders verify to build-before-test, which also stops local verify from silently testing a stale `dist/`. Validated with `rm -rf dist && pnpm verify`: 178 files / 2,063 tests green. This bug is exactly why Phase 0 exists.
- **Tag `v2026.6.12` moved to the fixed commit** (safe: no artifact had been created) and the workflow ran clean. **GitHub Release `v2026.6.12` published 2026-06-12T12:34:32Z** with `install.sh` + `twentytwohundred-2200-2026.6.12.tgz` attached ... the first release in 2200's history. npm publish self-skipped with the documented warning (no `NPM_TOKEN`).

## Incident worth remembering

One of the read-only review subagents edited `pub-lifecycle.ts` mid-review (changed the `'doug'` default to `'user'` + TODO) instead of just reporting it. Caught it via an unexpected prettier failure, reverted. The pub-owner default gets fixed properly in Phase 1 with a test, not smuggled through a release PR.

## Doug's open unblock

**`NPM_TOKEN`.** Create the npm account/org for the `@twentytwohundred` scope (under `dh@2200.ai` per convention), mint an automation token, add it as the `NPM_TOKEN` repo secret. Then we re-run the release workflow on the `v2026.6.12` tag and the install one-liner works end to end. Until then: GitHub Release exists, npm step self-skips with a warning.

## Next session

1. If `NPM_TOKEN` landed: re-run the release workflow on the `v2026.6.12` tag (publish step reads current secrets), verify `npm view @twentytwohundred/2200`, then clean-box install test (Docker `node:22` for Ubuntu; fresh macOS user account) following the README exactly.
2. Phase 1 must-fixes: `WebTokenStore` mode check, pub-owner default, tests for `tools/perm` + connector inbound routing.
3. Epic map refresh to v0.8 (Grok-First arc, connector Phases 1+2, embassy/shelf, the release plan's phase structure).
4. Backfill handoff for the 05-22 → 06-11 stretch from git history + CHANGELOG.
5. Workstream W: draft homepage copy + IA for Doug to react to.

## Evening continuation ... npm publish lands, with a fight

Doug set up npm same-day (user-to-org conversion: org `twentytwohundred`, personal account `dhardman2200`, granular token with bypass-2FA as `NPM_TOKEN`). Three obstacles between the token and a published package, all resolved:

1. **CalVer extended to `YYYY.MDD.HHMM`** ([[2026-06-12-calver-versioning]] v2). Doug wanted four-segment room (`2026.6.12.1234`); npm rejects four segments, so month+day pack into the minor slot and UTC cut time into the patch slot.
2. **2FA enrollment didn't survive the org conversion.** The new personal account had `two-factor auth: disabled` while Doug's authenticator still held the old account's entry ... his valid-looking codes meant nothing. Diagnosed via `npm profile get`; re-enrolled.
3. **npm blocks all-numeric package names.** The release run failed with `400 That word is not allowed` on `@twentytwohundred/2200`. Probe-confirmed with minimal stubs: exact name `2200` rejected, `2200-cli` and `cli` both clean. No allowlist path exists (npm's name-claim process is trademark-only; Doug holds a trademark filing on `twentytwohundred` if squatting ever matters). **Package permanently renamed to `@twentytwohundred/2200-cli`** (#270); binary, one-liner, paths unchanged. `@twentytwohundred/cli` held as a claimed alias.

**End state: `@twentytwohundred/2200-cli@2026.612.1935` is live on npm with provenance, dist-tag `latest`.** GitHub Release v2026.612.1935 created by the workflow; the broken v2026.612.1857 release+tag deleted; v2026.6.12 (GitHub-only) annotated as superseded. Clean-box validation passed in fresh `node:22` containers, root AND non-root (the non-root run exercised #233's `~/.npm-global` auto-fix): install → `2200 --version` → `2026.612.1935`.

**Phase 0 exit criterion: one step from done.** `https://2200.ai/install.sh` still serves the pre-rename script ... Simon redeploy requested at [[inbox/simon/2026-06-12-install-sh-redeploy]] (recommended: auto-sync from the latest GitHub Release asset). Once that lands, the public one-liner works end to end for strangers.

**Watch-item for Phase 1:** two more timing-marginal tests flaked locally under full parallel suite load (scheduler-integration `last_fired_at`, pub client WebSocket welcome) ... both 3-for-3 green in isolation, both passing on CI. Same family as the #234 chaos race. Worth one wait-predicate hardening pass over the integration tests rather than three more whack-a-mole rounds.

**Registry hygiene, low priority:** probe stub `@twentytwohundred/2200-cli@0.0.0` sits beneath the real version (harmless; could be unpublished with Doug's OTP inside the 72h window), and `@twentytwohundred/cli@0.0.0` is the claimed-alias stub (keep).

— Hobby (session 38, 2026-06-12 ... morning: first release ever; evening: first npm release)
