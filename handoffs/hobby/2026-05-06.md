---
agent: hobby
date: 2026-05-06
session: 9
canonical_path: wiki/handoffs/hobby/2026-05-06.md
---

# Hobby's session 9, 2026-05-06

Build-without-Doug day. Yesterday's queue was 11B → 12B → 14B → 15B; the spec dependency reordered that to 12B → 11B (Skills wrap as Extensions, so 12B's install lifecycle has to exist first). Five PRs shipped end-to-end across the day: PR #115 (12B substrate), PR #116 (11B substrate), PR #117 (12B-2 schedule registration with tick hook), PR #118 (11B-2 AgentLoop skill.invoke), PR #119 (Epic 14 transcript persistence + --replay). The first four MERGED; #119 is OPEN with CI starting.

When Doug came back mid-day to say "lots of daylight left," the queue rolled forward into Phase B-2 work. Both Skills and Extensions Phase B are now CLOSED; Epic 14's persistence + replay closure is in flight.

## What shipped

### Epic 12 Phase B substrate (PR #115, MERGED)

The `2200 extension install / uninstall / update` lifecycle on top of the Phase A registry. The shape is the in-process v1 with permission-kernel-chokepoint that `wiki/prior-art-analysis.md` §Epic 12 calls out: child-process boundary, strictly whitelisted env, captured stdio, 30s timeout, microVM future replaces the boundary as a substitution rather than a redesign.

What landed:

- **CLI verbs**: `extension install <path | github:owner/repo | https://github.com/...>` with `--yes` / `--force`, `extension uninstall <name>` with `--yes`, `extension update <source>` with `--yes` / `--allow-same-version`.
- **Hook executor** (`src/runtime/extensions/hooks.ts`): whitelist `PATH/HOME/LANG/LC_ALL/TZ` only, no parent secrets passing through; capability-derived env (`EXTENSION_NAME`, `EXTENSION_PERMS`, `EXTENSION_SCRATCH_DIR` only when granted, `EXTENSION_FROM_VERSION/TO_VERSION` for update); cwd = install dir; SIGTERM → 2s grace → SIGKILL with detached process group so bash-forks-sleep doesn't leak past timeout; stdout+stderr captured to `<home>/state/extensions/<name>/<hook>.log`.
- **Permission grants** (`src/runtime/extensions/grants.ts`): persisted at `state/extensions/<name>/grants.json`, Zod-validated, name-vs-dir checked. Runtime intersects manifest declarations with persisted grants, so an Extension cannot escalate post-install.
- **Per-Extension state** (`src/runtime/extensions/state.ts`): 1 MiB JSON bag at `state/extensions/<name>/state.json`, plus optional `scratch/` directory when `fs.scratch` granted.
- **Source resolution** (`src/runtime/extensions/source.ts`): local dirs (absolute / relative / `~`) and public GitHub URLs (`https://github.com/owner/repo` and `github:owner/repo` shorthand) via `git clone --depth=1`. Other sources throw `UnsupportedSourceError` ... npm + marketplace land in a later sub-phase.
- **Install orchestrator** (`src/runtime/extensions/install.ts`): copy → grant → init state → run hook → roll back on failure; backup-then-swap-then-hook with revert for update; teardown-after-hook for uninstall (always removes files even when the hook fails ... a misbehaving Extension cannot pin itself in place).

58 new tests on top of the 1054 that passed yesterday. End-to-end CLI smoke against the built bundle: install with permissions surfaced, capability env exposed to hook, log captured, uninstall hook ran, state torn down. Sample log:

```
hook=install  version=0.1.0  cmd="bash .../install.sh"
install hook for smoke-ext v0.1.0
perms: fs.scratch,network
scratch: .../state/extensions/smoke-ext/scratch
exit=0 signal=null duration_ms=7 timed_out=false
```

### Epic 11 Phase B substrate (PR #116, OPEN)

User-facing `2200 skill install` / `uninstall` verbs that mirror the Extension lifecycle, plus the in-memory synthesizer that wraps a ParsedSkill in an `ExtensionManifest` shape so the AgentLoop can consume Skills and Extensions through the same code path in the next sub-PR.

What landed:

- **`2200 skill install <source>`** with `--force`. Reuses the Extension source resolver (local dirs + public GitHub). Validates SKILL.md, copies the directory (incl. sibling reference files) into `<home>/skills/<name>/`, re-reads via the existing registry to confirm.
- **`2200 skill uninstall <name>`** with `--yes`. No hooks (Skills are pure-data at v1).
- **`synthesizeSkillManifest(skill)`** (`src/runtime/skills/wrapper.ts`): builds an in-memory `ExtensionManifest` from a `ParsedSkill`. Maps `tools` → `ExtensionTool[]`, derives Title-cased `display_name` from kebab slug, falls back through `extras.author` → supplied default → `'unknown'`. Result passes `ExtensionManifestSchema` so any code path that already speaks Extensions can consume Skills without special-casing.
- **`findSkillExtensionConflicts(home)`**: reports slugs that exist as both Skill and Extension. Helper for the AgentLoop wiring next sub-PR (the "rename one" message).

18 new tests. End-to-end smoke (install / list / show / uninstall) all green against the built CLI for a typical SKILL.md with frontmatter, tags, tools, and body.

### Epic 12 Phase B-2 (PR #117, MERGED)

Closes the Extensions framework on the schedule side. Decision record at `wiki/decisions/2026-05-06-extension-schedules-fire-tick-hook.md` (decide-and-tell call: tick hook over alternatives like synthetic-task-on-primary-agent or broadcast-to-fleet). What landed:

- **Manifest**: `hooks.tick?: string` (additive; non-breaking schema). Runtime refuses to install Extensions that declare schedules without `schedule` permission OR without a tick hook.
- **Hook executor**: `tick` is a fourth `HookKind`, same isolation contract as the others (whitelisted env, detached process group, 30s timeout, captured stdio). Adds `EXTENSION_SCHEDULE_ID` to the capability env on tick.
- **Schedule persistence** (`src/runtime/extensions/schedules.ts`): one JSON file per schedule at `<home>/state/extensions/<name>/schedules/<id>.json`. Cron validation reuses the per-Agent scheduler's parser. Reconcile-on-update preserves `last_fired_at` + `enabled` for unchanged ids.
- **Scheduler service**: scans `listAllExtensionSchedules` at start/reload alongside per-Agent schedules. Composite job key `extension:<name>:<schedule_id>` disambiguates the timer pool. `fireExtension` runs the tick hook (gated by `hasGrant('schedule')` ... defends against post-install permission revocation), records the fire, re-arms.
- **Install orchestrator**: schedule permission required when manifest declares schedules; tick hook required when manifest declares schedules; reconcile on install/update; clear on uninstall. Surfaces `schedulesChanged` so the CLI can RPC the supervisor for live reload.
- **`cli.scheduler.reload` RPC**: CLI calls it after extension install/uninstall/update when schedules changed; supervisor reload re-arms timers without a daemon restart.

30 new tests on top of #116's 1130. Tool registration deferred (the `tools[]` field meaning is not yet locked between "Extensions bring MCP servers" vs. "Extensions depend on existing tools").

### Epic 11 Phase B-2 (PR #118, MERGED)

Closes Skills Phase B by wiring the `SkillProvider` into the AgentLoop hot path. What landed:

- **`SkillProvider` seam** (`src/runtime/skills/provider.ts`): `list()` / `resolve(name)` / `conflicts()`. Default `FilesystemSkillProvider` reads `<home>/skills/` and `<home>/extensions/`.
- **AgentLoop integration**: optional `skillProvider`. When present, system prompt grows an "Available skills" section listing names + descriptions plus a "Name conflicts" note for slugs that overlap with Extensions. `skill.invoke` tool calls intercept before the dispatcher with three branches (not installed → error message, missing tool dep → error message, ok → SKILL.md body wrapped as the tool result). Without a provider, `skill.invoke` falls through to the dispatcher and fails with `ToolNotFoundError`.
- **`AgentProcess`**: every live `AgentLoop` gets a `FilesystemSkillProvider` so newly-installed skills are immediately invocable without an Agent restart.

11 new tests. Plan/run/perm records for `skill.invoke` are NOT yet written ... a future PR adds Skill-aware records.

### Epic 14: persist transcripts + spawn --replay (PR #119, OPEN)

The interview transcript was discarded after the spawn flow extracted the LLM-produced summary into the continuity-from-onboarding brain note. The summary is what the Agent reads on first run; the transcript is what the operator reads when wondering how the Agent got its shape. PR #119:

- **`src/runtime/onboarding/transcript-store.ts`**: `saveTranscript` / `loadTranscript` / `listTranscripts`. Storage at `<home>/state/onboarding/transcripts/<agent_name>-<file-safe-iso>.json`.
- **`2200 spawn`**: persists transcript on successful `migrateFromHandoff` (path appears in success summary; persist failure is a warning not fatal).
- **`--replay <path>`**: skips the interview entirely, loads saved JSON, runs the rest of the pipeline. Useful for testing onboarding-script changes against deterministic input or reproducing a destroyed Agent. Note: spec calls for `--from-script`, but that conflicts with the existing `--script` flag for question-script overrides; PR documents the rename.

9 new tests. CI starting at handoff write-time.

## State of the 19-epic map after today

Closed today:
- **Epic 12 Phase B** ... full lifecycle (install / uninstall / update + capability-derived hook execution + schedule registration via tick hook + supervisor live-reload RPC). Tool registration deferred (manifest-tools-meaning ambiguity flagged for the next design pass).
- **Epic 11 Phase B** ... full lifecycle (skill install / uninstall + ExtensionManifest synthesizer + AgentLoop `skill.invoke` interception + permission resolution + conflict surfacing in system prompt). Plan/run/perm records for skills deferred (named in code comment).
- **Epic 14 (Phase A enhancement)** ... transcripts persisted + `--replay` flag (PR #119 OPEN). Closes the persistence + replay gap that yesterday's queue called out.

Still in the build-without-Doug list (priority order):
1. **Epic 15 Phase B** ... web app deeper screens, including Pulse-in-UI consumption.
2. **Epic 7B** ... Inbox routing into notifications.
3. **Epic 12 Phase B tool registration** ... blocked on the manifest-tools-meaning decision (Extensions-bring-MCP-servers vs. Extensions-depend-on-tools).
4. **Skill plan/run/perm records** ... small follow-up to PR #118; gives Skills the same Brain-records discipline as native tool calls.

Doug-needed items still in the queue (none blocking):
- WS auth path fix for `?token=` query param (small Phase D-of-9 follow-up; cosmetic for Pulse-in-UI).
- Subprocessors page on `2200.ai/subprocessors` (only when managed service launches).
- Eventual Google publisher verification submission (when the unblock condition fires).
- Eventual mailing-address fill on legal docs (when LLC paperwork lands).

## Notes for future-me

- The hook executor's detached-process-group kill is load-bearing and surfaced as a real bug only because of the `bash + sleep 30` test case. Worth keeping in mind for any other place we spawn user-supplied scripts: SIGTERM to bash does NOT propagate to the children it execs, so killing the immediate child is insufficient.
- The `synthesizeSkillManifest` function is a forward-looking helper. It's tested but has no caller yet ... that lands in Phase B-2's AgentLoop wiring.
- `findSkillExtensionConflicts` likewise has tests but no caller yet. Same lands-with-AgentLoop-wiring pattern.
- The Extension install orchestrator's `validateApproval` rejects approvals that include permissions not in the manifest. This is a defensive check ... a buggy CLI handing the orchestrator an inconsistent decision should fail fast. Tests cover this so future edits to the CLI prompt flow can't regress it.
- Source resolution's GitHub support uses `git clone --depth=1`. Public repos only at v1. Private repos + non-GitHub hosts wait until per-source token storage lands alongside the marketplace work.

## What landed in GitHub today

| Repo | PR | Squash commit | State |
|---|---|---|---|
| `twentytwohundred/2200` | #114 | 9c46f12 | MERGED (this morning) |
| `twentytwohundred/2200` | #115 | 754d6f1 | MERGED |
| `twentytwohundred/2200` | #116 | fb65eb8 | MERGED |
| `twentytwohundred/2200` | #117 | 7275a0c | MERGED |
| `twentytwohundred/2200` | #118 | 4847214 | MERGED |
| `twentytwohundred/2200` | #119 | (pending) | OPEN, CI starting |
| `twentytwohundred/wiki` | (this handoff + decision record) | b844b7c, 1e2d8df, ... | pushed |

## What's next

Tomorrow's priority order, build-without-Doug:

1. **Epic 15 Phase B** ... web app deeper screens, including Pulse-in-UI consumption (the rendering work session 8's smoke flagged is still missing in Phase A).
2. **Epic 7B** ... Inbox routing into notifications.
3. **Skill plan/run/perm records** ... small follow-up: give `skill.invoke` calls the same Brain-records discipline as native tool calls.
4. **Manifest-tools-meaning decision** ... Doug-needed call: do Extensions bring their own MCP servers, or just declare dependencies on existing runtime tools? The answer unblocks Epic 12 Phase B tool registration.

Doug-needed items still in the queue (none blocking tomorrow):
- WS auth path fix for `?token=` query param (small Phase D-of-9 follow-up; cosmetic for Pulse-in-UI).
- Subprocessors page on `2200.ai/subprocessors` (only when managed service launches).
- Eventual Google publisher verification submission (when the unblock condition fires).
- Eventual mailing-address fill on legal docs (when LLC paperwork lands).
- The manifest-tools-meaning call above.

After today, 2200 has an actual Extension ecosystem: install a Skill or Extension from a local dir or public GitHub URL, see permissions surface at install time, watch hooks run with capability-derived env, watch schedules fire on cron, watch the model select and invoke skills mid-task with permission resolution surfacing missing tool deps. That's a meaningful "the substrate is real" milestone.

## Reflection

Five PRs in one day is a step up from the recent two-or-three cadence, but each landed verify-clean with end-to-end smoke and tight scope. The discipline that made it possible:

1. **Decide-and-tell on the schedule firing model.** The Extension manifest's `schedules[]` had no defined firing semantic. Three reasonable options (synthetic-task-on-primary-agent / broadcast / tick hook). Picking tick hook + writing the decision record + shipping the implementation in one motion is the decide-and-tell feedback memory in action.
2. **Substrate-then-integration split.** The two-PR-per-epic split (substrate, then registration / AgentLoop wiring) kept each PR reviewable and let me ship four big PRs without crossing 800 lines on any of them.
3. **Live-reload RPC pattern.** `cli.scheduler.reload` is the right shape for "CLI mutates disk, daemon re-scans." Easy to extend to other future surfaces (extension-tools registration, when that lands).
4. **Verify-clean at every commit.** Every `pnpm verify` step caught real things ... typecheck/lint/test/build all earned their existence today.
5. **End-to-end smoke against the built CLI.** Caught a subtle race in the timeout test (bash forks sleep, SIGTERM to bash leaves sleep alive holding stdio open) that unit tests would not have surfaced. The detached-process-group fix is a real product hardening, not just a test fix.

Tomorrow: Epic 15 Phase B is the next big surface. Pulse-in-UI consumption is the most user-visible piece — the dot starts moving in the browser, the fleet view becomes live.
