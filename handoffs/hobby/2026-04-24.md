---
agent: hobby
date: 2026-04-24
canonical_path: wiki/handoffs/hobby/2026-04-24.md
note: Living at flat path until Doug sets up github.com/twentytwohundred/wiki and restructures.
---

# What I did this session

- Read CLAUDE.md and all wiki docs (vision, architecture, epic map, seed team, the two decision docs, Simon's arrival inbox).
- Surveyed OpenClaw via repo README, VISION.md, SECURITY.md, `docs.openclaw.ai/llms.txt`, and the `skills/` directory listing. Sourced Perplexity Computer material from five third-party reviews (cybernews, bycrawl, Towards AI, Medium, Releasebot).
- Drafted `wiki/prior-art-analysis.md` v0.1. Six required sections per CLAUDE.md, plus a top-level lessons section surfacing eight proposed epic-map changes.
- Stopped at the strategic-framing checkpoint before going deeper to source reading. Doug confirmed that was the right call.

# What's in flight

- `prior-art-analysis.md` is at v0.1. Needs v0.2 to fold in the Skill compatibility pipeline decision (below), arch changes from the epic-map walkthrough, and balanced Perplexity Computer sources (their own technical writeups alongside the critical reviews).
- Epic 11 scope needs updating to reflect the parse → validate → normalize → disclose → install pipeline.

# Locked decisions from this session

1. **Skill compatibility is take-and-normalize with disclosure.** Not strict, not lenient. Pipeline: parse the SKILL.md, validate against spec, normalize known issues and log every change, notify the user of what was cleaned up and why with a link to review the modified version, install as a minimal Extension. This is a real architectural decision, not a footnote. Goes into the prior-art doc at v0.2 and into Epic 11's scope on the next epic-map update.
2. **No folder restructuring in wiki/ yet.** Keep writing flat files (`decision-*`, `handoff-*`, `inbox-*`) until Doug sets up `github.com/twentytwohundred/wiki` and pushes the initial commit. Restructure after that, not before.

# Sequencing for next session

1. **Walk through the eight epic-map changes with recommendations.** For each: what changes (new epic, scope addition, restructure), where it lands in the dependency graph, what to defer if Doug pushes back.
2. **Lock decisions on the three Doug called out specifically:**
   - **Cost caps as a runtime concept.** Where budget lives (Agent / Project / Instance), behavior when hit (pause, notify, slow down), how user overrides mid-task. Full spec needed.
   - **Runtime upgrade story.** Own epic, or sections inside Epic 16 (managed service) and Epic 17 (dogfooding). Recommendation with a defense.
   - **Provider-plugin SDK in Epic 10.** Split into 10a (SDK) + 10b (UX) or keep as additive scope. Defend the call.
3. **Update `prior-art-analysis.md` to v0.2.** Add Skill compatibility pipeline as locked architectural decision, fold in epic-map walkthrough outcomes, add Perplexity's own technical writeups for balance.
4. **Update `03-epic-map.md`** to reflect the locked changes.
5. **Then source reading.** Clone OpenClaw locally, pull 5-10 representative community Skills, study `src/agents/` and `src/gateway/`. Output is v0.3 of `prior-art-analysis.md`.
6. **Then draft Epic 2 spec** into `wiki/epics/02-agent-runtime-minimum.md`.

# Open threads

- `github.com/twentytwohundred/wiki` is being set up this session by Doug. Once pushed, the flat files here restructure into the nested layout from `04-seed-team.md` (`inbox/simon/`, `handoffs/hobby/`, `decisions/`, `epics/`). This handoff doc's canonical destination is `wiki/handoffs/hobby/2026-04-24.md`.
- Perplexity Computer source balance is a real gap. Current sources are 5-to-0 critical reviews. Need Perplexity's own material (official docs, technical writeups, dev-facing writeups) before v0.2 so the critique is of the actual design, not just user frustration.
- No inbox messages to Simon or Poe this session. First inbound was Doug's message to Simon (`inbox-simon-arrival.md`). Nothing on my side requires infrastructure coordination yet.

# What's next

Next session opens with the eight-change walkthrough. I'll bring a written recommendation for each, dependency-graph position, and defer-fallback. The cost-caps, runtime-upgrade, and plugin-SDK-split recommendations will be the meatiest. After Doug locks those three, the rest of the walkthrough should move fast.

---

*Handoff by Hobby · 2026-04-24*
